name: Nightly Live Continuous Integration

on:
  push:
    branches: [live]
  
jobs:
  build_website:
    runs-on: self-hosted
    steps:
      - shell: bash
      with:
        super_secret: "fnumgoin"
      run: |
        cd ~/nightlyjs
        pwd
        ls
        pm2 status
        git pull 