name: Nightly Live Continuous Integration

on:
  push:
    branches: [live]
  
jobs:
  build_website:
    runs-on: self-hosted
    steps:
      - run: cd ~/nightlyjs
      - run: pwd
      - run: ls
      - run: pm2 status